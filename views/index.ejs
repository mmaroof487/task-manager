<!DOCTYPE html>
<html lang="en">
 <head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Task Manager</title>
  <script src="https://cdn.tailwindcss.com"></script>
 </head>
 <body>
  <div class="text-white text-3xl bg-zinc-900 px-10 pt-4">Task Management</div>
  <div class="w-full min-h-screen bg-zinc-900">
   <div class="form p-10 text-white">
    <form action="/create" method="post" onsubmit="">
     <input type="text" name="title" placeholder="Title goes here" class="block px-5 py-2 bg-zinc-700 w-full focus:outline-none focus:bg-zinc-800" />
     <textarea name="details" id="" placeholder="Content goes here" class="mt-2 block px-5 py-2 w-full focus:outline-none focus:bg-zinc-800 bg-zinc-700 resize-none"></textarea>
     <input type="submit" class="hover:cursor-pointer px-5 py-2 rounded-md bg-blue-700 mt-2 p-6 align-center" value="Submit" />
    </form>
   </div>
   <div class="tasks flex gap-4 p-10 text-white">
    <% if(files.length>0){ %> <% files.forEach( (val)=>{ %>

    <div class="task w-72 px-3 py-4 rounded-md bg-zinc-700">
     <h1 class="text-lg font-bold"><%= val %></h1>

     <div class="flex gap-5">
      <a href="/file/<%=val%>" class="inline text-blue-600 hover:underline">Read more...</a>
      <a href="/edit/<%=val%>" class="inline text-blue-600 hover:underline">Edit</a>
     </div>
    </div>

    <% }) %> <% } else { %>
    <h3 class="text-zinc-600">No tasks yet</h3>

    <% } %>
   </div>
  </div>
 </body>
</html>
